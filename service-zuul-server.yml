spring:
  application:
    name: service-zuul-server
eureka:
  client:
    register-with-eureka: true
    instance:
      appname: service-zuul-server
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: service-jobad-reader
      stripPrefix: false
server:
  port: 8766
